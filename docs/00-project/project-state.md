# Project State

*Last updated: 2026-08-18. This is the one note that should change constantly — update it whenever the phase, decisions, or open questions shift. Everything else in this vault should change slowly. See [Action Tracker](action-tracker.md) for the current, deduplicated checklist of what's actually outstanding — this note is the narrative history behind it, and can go stale/redundant in places as items get resolved; the tracker is what wins if the two disagree.*

## Current phase

**No longer a mock-only frontend — the app now has a real, mostly-uncommitted, working feature set well past the original onboarding slice.** Committed on `develop`: LinkedIn onboarding (`b727986`), Level 2/3 verification + session-refresh wiring fix (`b94ad1d`, `c15cf44`). **Uncommitted, but built and functioning as of 2026-08-18**: the full meetup-scheduling slice ([ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md) — `services/meetup` backend, Schedule flow, browse-open-meetups replacing the old mock Matches tab, My Meetups with host accept/reject, the meetup detail page with the full Safety Gate), and a platform-split location picker (Apple MapKit on iOS, Stadia Maps on Android as a provisional testing choice — see [Testing Notes](testing-notes.md)/`TESTING-NOTES.md`). A hardcoded OTP testing bypass (`123456`) is also active — see the same testing-notes pointer; **must not ship**. App Store/Play Store compliance requirements were researched and gap-checked against the real app for the first time ([App Store & Play Store Compliance](../07-research/app-store-and-play-store-compliance.md)) — several real, currently-unmet requirements found (account deletion, a published privacy policy, Sign in with Apple), tracked in [Action Tracker](action-tracker.md) §2. None of this uncommitted work has been committed to git yet — that's the most concrete immediate next step.

## Completed

- Initial market/problem/competitive research ([Competitive Landscape](../01-product/competitive-landscape.md), [Sources & Citations](../07-research/sources-and-citations.md)).
- Core business requirements drafted ([Requirements](../01-product/requirements.md)).
- Verification architecture and trust-level model designed ([Verification Model](../02-domain/verification-model.md), [Trust Levels](../02-domain/trust-levels.md)).
- Deep threat-modeling and trust & safety architecture pass ([Threat Model](../03-architecture/threat-model.md), [Trust & Safety Architecture](../03-architecture/trust-and-safety-architecture.md), [Safety Features Catalog](../03-architecture/safety-features-catalog.md), [Privacy & Anti-Abuse Controls](../03-architecture/privacy-and-anti-abuse-controls.md)).
- Legal review of work-email verification approach under Sri Lanka's PDPA ([Sri Lanka PDPA - Work Email Verification](../07-research/sri-lanka-pdpa-work-email-verification.md)).
- Seven foundational decisions recorded as ADRs (`04 - Decisions`), including the progressive LinkedIn-first trust-onboarding redesign ([ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md)) and Flutter as the confirmed cross-platform frontend, Android + iOS ([ADR-007](../04-decisions/adr-007-flutter-as-the-cross-platform-frontend.md)).
- Added "The Overworked Professional" persona and sharpened the vault's professionals-only, safety-first positioning ([Vision](vision.md), [Personas](../01-product/personas.md)).
- Flutter mock-frontend code review (Cowork, 2026-08-16): overall well-structured (service-contract pattern, isolated Riverpod wiring, pure validators mirrored server-side, disciplined `mounted` checks). Findings: `IntentType`/`UserProfile` only model trust levels 1 and 4, no level 2/3 or federated-vs-claimed LinkedIn; a few hardcoded colors bypass `AppPalette`; most widget/feature files have no doc comments; test coverage thin (no tests for `MatchingService` or trust-gating logic).
- Added `.github/workflows/flutter-ci.yml` (format check + `flutter analyze --fatal-infos` + `flutter test --coverage` on push/PR) and untracked an accidentally-committed `run_log.txt`.
- Exported this vault's notes into the repo at `docs/` (mirrored folder structure, wikilinks converted to relative markdown links) and updated the repo's `CLAUDE.md` to point at them, plus a "Known Gaps" section. Sync is now a permanent, committed script (`scripts/sync_docs_from_vault.py`), not a one-off.
- Backend platform architecture decided and recorded: Go microservices on Cloud Run, custom gateway (behind a Google Load Balancer for TLS), gRPC for interservice calls, GCP Pub/Sub for eventing (not Kafka — Cloud Run can't host a persistent consumer), a dedicated Realtime Gateway service for WebSocket fan-out, and Cloud Run IAM invoker auth instead of a self-managed mTLS mesh ([ADR-008](../04-decisions/adr-008-backend-platform-architecture.md)).
- Auth strategy decided and recorded: RS256 JWT (asymmetric — only the auth service holds the private key), refresh-token rotation with replay detection, mobile secure-storage (Keychain/Keystore) vs. web httpOnly-cookie token storage split by client type, staff/admin auth kept fully separate from consumer auth ([ADR-009](../04-decisions/adr-009-jwt-auth-strategy.md)).
- Profile-visibility simplified: no user-managed public/private toggle; instead, how much of another user's profile you can see depends on your own subscription tier (free = limited card, paid = full profile) — folded into [Revenue Model](../01-product/revenue-model.md). Existing anti-stalking behavioral controls (rate-limited search, hide-from-company/industry) are unaffected and still apply.
- Database hosting decided: Cloud SQL for PostgreSQL (Enterprise edition, shared-core tier, single-zone to start, backups + PITR from day one), not AlloyDB (premium not justified yet), not self-hosted ([ADR-010](../04-decisions/adr-010-postgresql-hosting-on-cloud-sql.md)). Cloud Run connects via the Cloud SQL Auth Proxy.
- LinkedIn onboarding slice fully designed and scaffolded ([ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md)): PKCE-based LinkedIn OIDC flow (LinkedIn's own access token never persisted), REST-to-gRPC gateway translation, minimal `users`/`refresh_tokens` schema scoped strictly to Level 1a, repository pattern + `sqlc` for injection-proof queries, Redis-backed rate limiting on auth endpoints, minimal-PII `user.onboarded` Pub/Sub event. `backend/` created in the repo: `docker-compose.yml`, both services' `Dockerfile`s, SQL migrations, the `.proto` gRPC contract, `go.work`/`go.mod` files, `.env.example`, `README.md`, and `PLAN.md` (the file-by-file build brief for Claude Code).

## Currently working on

- **`frontend/PLAN.md` written (2026-08-16)** — built from [Feature Build Plan Template](../06-roadmap/feature-build-plan-template.md), the frontend counterpart to the now-merged backend slice: replaces `onboarding_flow.dart`'s pre-[ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md) phone/LinkedIn-URL/corporate-email wizard with a real LinkedIn OAuth+PKCE flow against the live `/v1/auth/linkedin/callback` contract, plus wiring `ProfilePage`'s already-present but stubbed "SIGN OUT" button to actually clear the session. Scoped strictly to Level 1a and mobile-only, same discipline as the backend. Surfaced two real gaps while writing it: nothing today persists a logged-in session across app restarts (no session provider exists, `SplashScreen` always routes to `LandingPage`), and there's no `GET /v1/users/me` endpoint yet, so cached profile fields from the one-time callback response are all the app will have until that's built. **Correction (2026-08-16)**: LinkedIn requires an absolute HTTPS redirect URL, not a custom URL scheme directly — built `auth-bridge/index.html` (branded, matches `AppPalette`, zero server logic, tap-to-continue fallback for browsers that block scripted custom-scheme redirects) plus `auth-bridge/README.md` (Firebase Hosting recommended for deployment). `frontend/PLAN.md` Step 0.2 and `backend/PLAN.md`'s flow description both updated to reference it (no backend code changes needed — `redirect_uri` was always just an opaque string to the Go code). Also pinned `frontend/PLAN.md` to use `url_launcher`'s `LaunchMode.inAppBrowserView` explicitly (Custom Tabs/`SFSafariViewController`) rather than an unspecified launch mode, after explaining the embedded-WebView-vs-in-app-browser-tab distinction (RFC 8252) — avoids accidentally landing on the insecure `inAppWebView` mode.
- **Plan error caught by Claude Code, fixed (2026-08-16)**: `frontend/PLAN.md` incorrectly claimed `fullName`/`profilePhotoUrl` come back in the `linkedin/callback` response — they don't (confirmed against the real `sessionResponse` struct); LinkedIn's data is fetched and stored on the `users` row but never threaded through to any response. Fixed the plan to specify the actual small backend addition needed (`full_name`/`profile_photo_url` as top-level `SessionResponse` fields, not the JWT — wrong place for one-time session data). Separately, `trustLevel` isn't a top-level field either — only in the JWT's claims — client-side decode (display-only, non-authoritative) confirmed as the right call. - **`frontend/PLAN.md` fully regenerated (2026-08-16)**, consolidating everything decided since the first draft into one clean document rather than another patch: mobile-only session storage, the `auth-bridge/` HTTPS redirect mechanics (with exact deploy commands), `LaunchMode.inAppBrowserView`, and the `full_name`/`profile_photo_url` fix promoted to its own explicit Step 3.
- **Full cross-check completed (2026-08-17): vault, actual code, and `frontend/PLAN.md`.** No security vulnerabilities found in the implemented code — confirmed clean: no token/secret ever logged, no hardcoded client secret, PKCE uses `Random.secure()` correctly with `state`/`verifier` as genuinely distinct values, the JWT `trustLevel` decode is isolated to exactly one function and fails safe to `0` rather than throwing, secure storage is the only place tokens touch disk, custom URL scheme correctly registered on both platforms. Found and fixed four real (non-security) drift/hygiene items: (1) `auth-bridge/.gitignore` didn't exclude Firebase CLI's bundled `.agents/` AI-assistant skill docs or `skills-lock.json` — `git add auth-bridge/` would have swept in dozens of irrelevant files, now excluded; (2) `auth-bridge/firebase.json` was deploying `skills-lock.json`/`README.md` to the live bridge URL unnecessarily — excluded; (3) `backend/ARCHITECTURE.md` was missing the `full_name`/`profile_photo_url`/JWT-`trust_level` documentation Step 3 called for — added; (4) `backend/.env.example`'s `LINKEDIN_REDIRECT_URI` placeholder still showed the custom scheme instead of the required HTTPS bridge URL, which would have misled a fresh setup — corrected with an explanatory comment. `frontend/PLAN.md`'s step numbering/cross-references checked internally consistent after multiple edits. ADR count references still accurate (011). Verdict: on the right track, no requirement mismatches found against [ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md)/[ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md)/[Trust Levels](../02-domain/trust-levels.md).
- **Steps 0-10 executed (2026-08-17), rechecked against the actual code, uncommitted.** `auth-bridge/` deployed and registered with LinkedIn (`https://professional-meetups-976d2.web.app`, matches `backend/.env`). The `SessionResponse` `full_name`/`profile_photo_url` addition (Step 3) **was made correctly** — confirmed in `backend/proto/auth/v1/auth.proto` and threaded through `service.go`/`handlers.go`, closing the earlier open question. Overall quality is solid: real PKCE `state` verification, idempotent sign-out, fail-safe session loading (corrupted Keystore entry → treated as no session, not a hang). **Correction (2026-08-17): `http_auth_service.dart`'s use of `LaunchMode.externalApplication` was initially flagged as a deviation from Step 1's `inAppBrowserView` spec — reconsidered after pushback and confirmed it's actually the right call, not a bug.** RFC 8252's real requirement is narrower than "use an in-app browser tab": just never an embedded, host-app-inspectable WebView. Both `externalApplication` and `inAppBrowserView` satisfy that — the difference is UX only, and `externalApplication` is simpler, more predictable across Android OEM browser variants, and the more common real-world pattern. `frontend/PLAN.md` updated to reflect `externalApplication` as the confirmed choice throughout. Added **Step 11** — trust/purpose microcopy above the LinkedIn button (Shashika's new request), with copy that stays accurate to the actual requested OAuth scope (`openid profile email` only, confirmed in the code — no posting/connections access, so "we never post on your behalf" is a true claim, not marketing). **Confirmed with Shashika**: web session persistence stays out of scope here — `flutter_secure_storage`'s web implementation is just obfuscated browser storage, not equivalent to Keychain/Keystore, and [ADR-009](../04-decisions/adr-009-jwt-auth-strategy.md) already calls for httpOnly cookies on web instead, which needs gateway changes (platform-aware token issuance) and a web-compatible OAuth redirect that don't exist yet. Web gets its own slice later. Not yet handed to Claude Code.
- **LinkedIn onboarding slice ([ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md)) — code complete, reviewed, committed, pushed (2026-08-16).** Claude Code executed `backend/PLAN.md` Steps 0-7 in full: 53 tests across `shared/`, `services/auth`, `services/gateway`; a real integration test against live Postgres; `docker compose down -v && up --build` verified from a clean checkout with a real end-to-end LinkedIn-callback request traced through gateway → gRPC → auth. Cowork independently reviewed the actual diff (not just the plan): confirmed no token/secret ever logged, all SQL parameterized via `sqlc`, the gateway module contains zero references to `shared/jwt` (structurally cannot construct a signer, only verify), rate limiting wired into `main.go`, the migration matches [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) exactly (no speculative Level 2-4 columns), and `secrets/` is genuinely gitignored, not just assumed to be. Committed as `b727986` ("Implement LinkedIn onboarding slice: gateway + auth services, [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md)", 74 files) and pushed to `origin/develop`. One non-blocking follow-up, deliberately not filed as a task, tracked here instead: `CompleteLinkedInOnboarding` doesn't retry the lookup if `Create()` returns `ErrConflict` from a concurrent-registration race — fails safe, low-probability (LinkedIn auth codes are single-use), pick up opportunistically rather than as a scheduled task.
- **Repo reorganization into `frontend/` + `backend/`**: with two independent toolchains now in one repo, the Flutter app is being moved from the repo root into `frontend/` (mirroring `backend/`) for a clean monorepo split. `CLAUDE.md` and `.github/workflows/flutter-ci.yml` are already updated to expect this layout (Flutter root at `frontend/`, CI `working-directory: frontend`, coverage-artifact path updated). A one-time migration script (`scripts/migrate_frontend.sh`) does the actual `git mv` of `lib/`, `test/`, `android/`, `ios/`, `macos/`, `web/`, `assets/`, `pubspec.yaml`/`.lock`, `analysis_options.yaml`, `.metadata` into `frontend/`, splits the Flutter-specific `.gitignore` rules into `frontend/.gitignore`, and trims the root `.gitignore` to repo-wide/editor/OS concerns only — still needs to actually be run and committed on the real repo. `docs/` stays at the repo root deliberately (applies to both sides).
- **Backend Go source doesn't exist yet — scaffolding does.** Next action is handing `backend/PLAN.md` to Claude Code to actually write, compile, and test the gateway and auth services against the already-created Docker/schema/proto scaffold. Prerequisites (installing Go, registering a LinkedIn OAuth app, generating a local JWT keypair) are called out at the top of that plan as human steps, not Claude Code steps.
- `onboarding_flow.dart` (frontend) still implements the pre-[ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md) trust model (phone → LinkedIn URL-paste → mandatory corporate email, sequential) — **known and deliberately not yet fixed**, flagged in the repo's `CLAUDE.md` "Known Gaps" section and here. Rebuild needs to happen alongside the real auth service (once built) rather than against the mock, so it's wired to the actual `POST /v1/auth/linkedin/callback` contract in `backend/PLAN.md` rather than guessed at twice.

- **Real-device testing surfaced two backend bugs, both root-caused and fixed in code, confirmed 2026-08-17.** (1) `invalid_scope_error` from LinkedIn — the "Sign In with LinkedIn using OpenID Connect" product wasn't yet added to the LinkedIn Developer app; fixed by adding it in the portal. (2) `401 invalid_client` on the token exchange — root cause was a genuine architecture mismatch, not a credential typo: [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) specified PKCE (`code_challenge`/`code_verifier`), but LinkedIn's self-serve "Sign In with LinkedIn using OpenID Connect" product doesn't support PKCE at all — PKCE only exists under a separate, gated "OAuth 2.0 for Native Clients" program (manual LinkedIn rep enablement, a different authorization endpoint, a loopback-IP redirect URI incompatible with the deployed `auth-bridge` HTTPS-page approach). **Fixed**: `code_challenge`/`code_verifier` removed from both the Flutter authorization request and the Go token exchange (`linkedin/client.go`'s `ExchangeCode` — see its doc comment for the confirmed root cause), `Pkce` renamed to `OAuthState` reflecting its narrower remaining purpose (still generates/verifies `state` for CSRF protection). Not a security regression — the backend is a confidential client holding `client_secret` server-side, which is the property PKCE exists to substitute for on a public client. ~~[ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) still needs a correction note~~ done — [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) now has an explicit correction section, PKCE bullet struck through, kept for historical record.
- **Security review (Claude Code, 2026-08-17) found one real Medium-severity issue — confirmed independently, fixed and verified in code.** `apperror.ToGRPCStatus` (`backend/shared/apperror/grpc.go`) now returns a generic `"internal error"` message for the `codes.Internal` case, logging full detail server-side via `slog` instead of exposing it; `service.go`'s LinkedIn-exchange error paths wrap with a fixed client-facing message ("linkedin sign-in failed, please try again") rather than embedding LinkedIn's raw response body, which is still logged server-side in full.
- **New slice scoped (2026-08-17): session UX + homepage personalization.** Session-restore-on-launch already works (`SplashScreen` checks `authSessionProvider` and routes to `AppShell` vs. `LandingPage` — built as part of the earlier onboarding rebuild, just needed confirming, not building). Net new work: (1) a confirmation dialog before sign-out (currently signs out immediately on tap, no confirmation); (2) `HomePage`/`HomeHeader` currently hardcode `userName: 'Shashika Fernando'` and never pass a profile-photo URL to the avatar — wire both to `authSessionProvider`'s real `UserProfile`; (3) `ProfilePage`'s "LinkedIn — Not connected" verification row is stale/wrong once real LinkedIn sign-in works (a logged-in user *is* LinkedIn-connected by definition) — fix to reflect actual state. **Company name from LinkedIn explicitly requested and explicitly deferred**: LinkedIn's self-serve OIDC product's `userinfo` response has no employer/company field at all (confirmed against LinkedIn's own schema docs) — that data requires LinkedIn's Partner Program (Marketing Developer Platform), a manual approval process typically taking weeks to months, not self-serve. Shashika chose to skip this for now rather than self-report it or pursue Partner access — revisit if/when there's a reason to pursue Partner Program access.
- **Level 2/3 verification slice scoped and decided ([ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md), 2026-08-17)**: phone OTP (Firebase Phone Auth, ID-token-verified server-side), personal email OTP (backend-generated code, transactional-email delivery, exact provider TBD), personal details (name/address, matches documented Level 2 exactly, not narrowed), corporate email MVP (OTP + domain capture only, fraud-detection stack deliberately deferred — see [Verification Model](../02-domain/verification-model.md) § 5's implementation-status note). All steps skippable with trust-nudge microcopy, same functionality also required in `ProfilePage` post-onboarding (its Phone/Work Email rows are already stubbed there, waiting on exactly this). Resolves the previously-open "single identity anchor" item below: `phone_number`/`personal_email` both get `UNIQUE` constraints. Meetup/Matching/Safety-Gate explicitly **not** part of this — Shashika confirmed it needs its own initiative given it requires a Matching service (doesn't exist) and the full Safety Gate flow, not simple CRUD; deferred, to be picked up next.

- **Level 2/3 verification slice planned (2026-08-17), building on the already-Accepted [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md) — including a same-day correction to it.** `backend/PLAN.md` and `frontend/PLAN.md` both got a full addendum: personal email OTP (backend-generated, `EmailSender` interface with a logging dev fallback until a transactional provider is chosen), personal details (legal name/address, self-reported), corporate email OTP (MVP-scoped per [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md) — free/role-based domain rejection only, no reputation checks/company DB/manual review). **Phone verification was reconsidered mid-planning**: [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md) originally specified Firebase Phone Auth; after confirming Firebase does genuinely support iOS (a passing worry that it might not was checked and is false — iOS just needs an extra APNs key upload), the real cost was the Blaze-plan billing requirement plus running a second, Firebase-managed OTP mechanism alongside the backend-owned one already being built for email. Corrected to route phone through the identical backend-owned OTP pattern as email instead (`SmsSender` interface, same `verification_codes` table). [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md) itself carries the correction note, not silently edited. **SMS provider chosen (2026-08-17): Twilio, plain Programmable Messaging API — deliberately not Twilio Verify**, which would have reintroduced the two-mechanism split this correction exists to remove. **Email provider also chosen (2026-08-17): Resend** — 3,000 free emails/month, no card required, good fit for a pre-revenue project; sandbox `onboarding@resend.dev` from-address works without domain verification for testing. `backend/.env.example` and `docker-compose.yml` both updated with empty `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_PHONE_NUMBER`/`RESEND_API_KEY`/`RESEND_FROM_EMAIL` placeholders for Shashika to fill in; `LoggingSmsSender`/`LoggingEmailSender` are the automatic fallbacks when empty. Both providers now decided — nothing left deferred in this addendum. **Full pre-handoff consistency audit done (2026-08-17)**: re-read both addenda end to end against [ADR-003](../04-decisions/adr-003-ephemeral-work-email-verification.md)/006/008/009/011/012, Trust Levels, Verification Model, and the actual current backend code (not assumed) — found and fixed six real gaps: `computeTrustLevel`'s Level-3 logic was ambiguous about requiring Level 2's conditions too (now explicit code, not prose); no handling for two users racing to verify the same phone/email (now maps to `ErrConflict`); the plan assumed the gateway already verifies JWTs when it actually doesn't yet (confirmed by reading the code — `JWT_PUBLIC_KEY_PATH` was wired in config but never consumed) — now flagged as new construction, not reuse; a redundant per-user rate-limit suggestion removed in favor of the already-sufficient `verification_codes` cooldown; `backend/ARCHITECTURE.md` update added as an explicit step (was missing); and a frontend gap where `UserProfile`'s verification booleans wouldn't refresh immediately after a successful verification (fixed: re-fetch `getProfile()` after every verify success, not just the token). All four steps individually skippable with trust-benefit microcopy, and independently reachable again from `ProfilePage` for anyone who skipped — resolved two open questions along the way: personal-details capture is included (matches [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md)'s Level 2 definition exactly, confirmed with Shashika rather than silently narrowed), and a real `GET /v1/users/me` finally gets built (this addendum is what actually needs it — `trust_level` is recomputed server-side after every verification step and a fresh token reissued immediately, so the UI doesn't wait for a natural refresh to reflect a new level). **Sequenced to run only after the in-flight Steps 12-15 diff is merged** — both slices touch `app_providers.dart`/`ProfilePage`, and two concurrent Claude Code sessions on the same files would produce a bad merge.
- **Meetup creation/scheduling explicitly deferred as its own future slice (2026-08-17, confirmed with Shashika)**: a different domain from onboarding/verification — per [ADR-008](../04-decisions/adr-008-backend-platform-architecture.md)'s "one service per bounded domain," this will need its own ADR (likely its own backend service, not bolted onto `services/auth`) and its own `PLAN.md`, scoped after the Level 2/3 verification slice lands, not bundled into it.
- **Level 2/3 verification and the session-refresh wiring fix — both committed (2026-08-18).** `b94ad1d` ("LinkedIn auth and multi step trust level") covers the full [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md) slice (phone/personal-email/corporate-email OTP, personal details, `GET /v1/users/me`, server-side trust-level recomputation). `c15cf44` ("Wire automatic session-token refresh, fixing forced re-login every 15 minutes") fixes the confirmed bug where `refreshSession()` was implemented but never called — a `TokenRefresher` (proactive, single-flighted to avoid the refresh-token-rotation replay trap) is now wired into both session restore and the `getAccessToken` callback, plus a `forceSignOut()` + `AppShell`-level listener for a mid-session refresh failure.
- **Meetup scheduling slice ([ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md)) — fully built, uncommitted (2026-08-18).** Host-initiated meetups with join requests, superseding Match→Meetup as the Phase 1 mechanism (the compatibility-matching engine stays Phase 3, reframed as a future discovery/ranking layer, not the creation path). Trust floor for hosting/joining raised from Level 1 to Level 2 for coffee/lunch/networking/mentorship (ride-share/dating stay at 4), mirrored server-side in `services/meetup/internal/service/trustgate.go`. Full Safety Gate (checklist → optional live-location → check-in → post-meetup feedback) wired in, not simplified. Capacity auto-rejects on overflow, verified safe under real concurrent requests (`TestCapacityRace_Integration`, actual goroutines). The mock `MatchesPage`/`MockMatchingService`/`MatchProfile` are fully retired, not left running in parallel — the Matches tab is now the real browse-open-meetups surface. Backend: new `services/meetup` microservice (own Cloud Run deployment, own Postgres tables, Pub/Sub → FCM push notifications with a `LoggingPushSender` fallback). Frontend: Schedule flow (today/later), My Meetups with host request management, meetup detail page with the full Safety Gate, `HomePage`'s "next meetup" card now wired to real data.
- **Map/location provider — split by platform, still partly provisional (2026-08-18, [ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md) §4's second and third corrections).** Originally scoped as Google Maps; switched to Mapbox after research; switched again after Shashika asked about free/no-card alternatives and about using the phone's own maps app directly. Landed on: **iOS uses Apple's native MapKit** (settled — free, no API key, no billing account, ships with the OS) via `apple_maps_flutter` + a small native `LocalSearchChannel.swift` platform channel wrapping `MKLocalSearchCompleter`. **Android stays on Stadia Maps** (`maplibre_gl`) as a provisional testing choice — no card required, but the final Android vendor (Stadia vs. Mapbox vs. Google) is still open, tracked in [Testing Notes](testing-notes.md)/`TESTING-NOTES.md`. A real address-search bug was found and fixed during this pass: not an API/network issue at all (methodically ruled out via `curl` replay and response inspection) — a `Stack` clipping/paint-order bug was silently hiding the suggestions dropdown, plus a second hit-test bug the fix's own new tests caught before it shipped. One open UX question Claude Code flagged rather than silently resolved: whether picking a suggestion should auto-advance the Schedule flow, or (as currently built) require the existing CONTINUE button as an explicit confirm step — needs a decision, not urgent. Manual on-device interactive testing of the search UI (real typing/tapping, not just automated tests + a non-interactive build/launch check) hasn't been done yet.
- **A hardcoded OTP testing bypass is active (2026-08-18) — must not ship.** Every phone/personal-email/corporate-email verification code accepts `123456` instead of the real generated code, added to exercise Level 2/3 flows without live Twilio/Resend sends. Single change in `backend/services/auth/internal/service/otp.go`, real comparison commented out not deleted. Full detail and exact revert steps in `TESTING-NOTES.md` (repo root) and [Testing Notes](testing-notes.md) (vault pointer). Five backend tests fail while it's active, by design — that's the tripwire.
- **App Store / Play Store compliance researched for the first time (2026-08-18)** — [App Store & Play Store Compliance](../07-research/app-store-and-play-store-compliance.md). Real, currently-unmet requirements found: account deletion (required unconditionally by both stores, nothing built beyond sign-out), a published privacy policy + accurate data-safety declarations (nothing exists), Sign in with Apple or an equivalent (iOS-specific, real trust-model design implications). Also surfaced that the already-flagged "Dating intent selectable but inert" scope gap (`CLAUDE.md`) is now also a Play Store age-verification policy question, not just a scope-discipline one.
- **A full security/design review was run twice this phase (2026-08-18)** — once against the committed code + in-progress session-refresh diff (no active vulnerabilities found; one hygiene item, a dead `crypto`/PKCE-era dependency in `frontend/pubspec.yaml`, still not cleaned up as of this writing), and this note's own update reflects a second pass covering the newer meetup-scheduling and location-picker code (see [Action Tracker](action-tracker.md) for anything that pass surfaced).

## Blocked / needs a decision from you

- **Level 1b product call**: confirm the [ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md) rule that a pasted-URL-only LinkedIn ("claimed," not federated) cannot match or be matched — this was set as the default by Cowork per the safety-first principle, but it's a real product trade-off (it means some users who won't do LinkedIn OAuth can browse but never actually use the app) and deserves an explicit yes from you.
- **Legal sign-off**: [ADR-003](../04-decisions/adr-003-ephemeral-work-email-verification.md) and [Sri Lanka PDPA - Work Email Verification](../07-research/sri-lanka-pdpa-work-email-verification.md) flag that making *any* verification step mandatory as a condition of using the service should be reviewed by a Sri Lankan privacy lawyer before launch. [ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md) makes this lower-stakes for work email specifically (now optional), but the baseline LinkedIn/personal-verification requirement would need its own version of this review if challenged.
- **Account-deletion retention tension**: soft-delete via an explicit `account_status` enum + PII anonymization is the agreed direction, but full anonymization conflicts with the Ban Evasion Prevention control's need to retain some non-reversible fingerprint after "deletion" so banned users can't trivially return. Flagged for the same category of legal review as the item above — not yet resolved.
- **Company allowlist ownership**: who manually curates/maintains the initial approved-company database (see [Verification Model](../02-domain/verification-model.md) § Company Verification Database)? Not yet assigned.
- **iOS permission strings**: `ios/Runner/Info.plist` has no usage-description keys yet (no location/camera plugins wired in yet). Needs `NSLocationWhenInUseUsageDescription`, `NSCameraUsageDescription`, etc. added — alongside the equivalent Android manifest permissions — before, not during, the SOS/live-location/KYC build-out. See [ADR-007](../04-decisions/adr-007-flutter-as-the-cross-platform-frontend.md).
- **GCP lock-in acknowledgment**: [ADR-008](../04-decisions/adr-008-backend-platform-architecture.md) commits Pub/Sub, Cloud Run IAM, and Cloud Run ingress specifically — worth a conscious "yes, we're fine with this" rather than discovering it as a surprise later if a multi-cloud need ever comes up.
- **Deployment region**: Mumbai (asia-south1) vs. Singapore (asia-southeast1) for Cloud Run + Cloud SQL, given the Sri Lanka launch and NFR-001's <200ms matching requirement. Not chosen — a real latency test would help decide.
- **RPO/RTO targets for Postgres**: [ADR-010](../04-decisions/adr-010-postgresql-hosting-on-cloud-sql.md) turns on backups/PITR, but that's not the same as an agreed recovery-point/recovery-time objective — needs an explicit business decision on acceptable data-loss/downtime windows, not just "backups are on."
- ~~**Single identity anchor risk**~~ — **resolved ([ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md), 2026-08-17)**: `phone_number` and `personal_email` both get `UNIQUE` constraints as part of the Level 2 migration, functioning as real secondary identity anchors. Still genuinely undecided and not part of [ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md): whether/how a user could later re-link a different LinkedIn account to the same Professional Meetups account.

## Backend platform — open technical items (tracked 2026-08-16, not yet decided or built)

These don't block starting the onboarding slice, but they're real gaps for a production app and shouldn't be discovered piecemeal later. None of this is silently assumed — it's tracked here on purpose.

- **Secrets management**: where the JWT private signing key ([ADR-009](../04-decisions/adr-009-jwt-auth-strategy.md)), LinkedIn OAuth client secret, and DB credentials actually live. GCP Secret Manager is the assumed answer but hasn't been decided or configured.
- **Backend CI/CD**: nothing yet builds a Go service, pushes an image, and deploys it to Cloud Run. The only CI that exists is the Flutter frontend's (`.github/workflows/flutter-ci.yml`).
- **Rate limiting / WAF**: the gateway is supposed to do rate limiting ([ADR-008](../04-decisions/adr-008-backend-platform-architecture.md)) but the algorithm and policy (per-IP vs. per-user vs. per-endpoint) aren't designed, and Cloud Armor hasn't been decided on.
- **Infra/AppSec threat coverage**: [Threat Model](../03-architecture/threat-model.md) covers *product* threats (fake profiles, scams, romance fraud) — it says nothing about container image vulnerabilities, dependency CVEs, or IAM misconfiguration. That's a distinct, currently uncovered category, separate from the product threat model and not yet started.
- **Backend test strategy**: no decision yet on unit/integration/contract testing across the gRPC services.
- **Cost monitoring**: no GCP budget alerts configured — worth doing given cost-avoidance was the whole reason for choosing GCP over AWS in the first place.
- **Realtime Gateway connection-registry design**: [ADR-008](../04-decisions/adr-008-backend-platform-architecture.md) establishes a dedicated Realtime Gateway service for WebSocket fan-out, but the actual mechanism for routing a Pub/Sub message to the *specific* gateway instance holding a given user's socket (e.g. a Redis-backed `user_id → instance_id` registry) isn't designed yet — only the general shape.
- **`subscriptions` table not yet built**: shape decided ([Domain Model](../02-domain/domain-model.md) § Subscription — own table, FK to `users`, tier + status, never a column on `users` itself), but correctly out of scope for the Level-1a-only onboarding slice ([ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md)). Build it as its own migration when Premium-tier work starts, not speculatively now.

## Next

1. ~~Install Go locally, register the LinkedIn OAuth app, generate the local JWT keypair.~~ Done.
2. ~~Hand `backend/PLAN.md` to Claude Code to write, compile, and test the gateway + auth services.~~ Done.
3. ~~Bring the resulting diff back to Cowork for an architecture/security review pass.~~ Done, approved.
4. ~~Commit and push the onboarding-slice diff.~~ Done — `b727986` on `origin/develop`.
5. ~~Apply the PKCE-removal fix and the error-message-redaction security fix.~~ Done, confirmed in code.
6. ~~Add an [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) correction note reflecting the PKCE removal.~~ Done.
7. ~~Build the session-UX + homepage-personalization slice.~~ Done, committed as part of `b94ad1d`.
8. ~~Build the Level 2/3 verification slice ([ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md)).~~ Done, committed (`b94ad1d`).
9. ~~Fix the refresh-token-never-called bug.~~ Done, committed (`c15cf44`).
10. ~~Scope and build meetup-scheduling as its own initiative ([ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md)).~~ Done, built, **not yet committed**.
11. **Commit the meetup-scheduling + location-picker diff** — the most concrete immediate next step; everything since `c15cf44` is sitting uncommitted in the working tree.
12. Decide the suggestion-tap-auto-advance vs. explicit-CONTINUE-button question Claude Code flagged in the location picker (`TESTING-NOTES.md`'s "Known caveats" for the platform-split pass) — not urgent, but a real UX decision, not a bug.
13. Do a real hands-on pass of the location picker's search UI on both platforms (typing, tapping suggestions, the keyboard search action) — only automated tests and a non-interactive build/launch check have verified it so far.
14. Start closing the App Store/Play Store compliance gaps ([App Store & Play Store Compliance](../07-research/app-store-and-play-store-compliance.md)) — account deletion and a published privacy policy are the two that would block submission outright; Sign in with Apple needs a real design decision, not just a UI addition, given the LinkedIn-anchored trust model.
15. Resolve the "Dating" intent's selectable-but-inert UI exposure (`CLAUDE.md`'s flagged scope gap) — now also a Play Store age-verification policy question, not just scope discipline; hiding it from the UI until Phase 2 is the cheap fix if that's the direction chosen.
16. Clean up the dead `crypto` package + stale PKCE comment in `frontend/pubspec.yaml`, flagged by the security review and still not done.
17. Resolve the still-open decision items in [Action Tracker](action-tracker.md) §2 (Level 1b product rule, legal PDPA review, GCP lock-in ack, deployment region, RPO/RTO, LinkedIn re-linking) and §1 (Maps/Firebase-adjacent prerequisites once the final map vendor is chosen, Twilio/Resend account creation).
18. Continue Phase 1 build per [Roadmap](../06-roadmap/roadmap.md) once the above settle: full safety-gate infrastructure beyond what [ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md) already delivered, reporting/blocking with real backend effect (now also an app-store submission requirement, not just a safety one), SOS/emergency flow.

## Change log

- **2026-08-16** — Vault created and populated from initial product/architecture/legal research docs.
- **2026-08-16** — Trust-level model redesigned to progressive/LinkedIn-first onboarding per [ADR-006](../04-decisions/adr-006-progressive-linkedin-first-trust-onboarding.md); updated [Trust Levels](../02-domain/trust-levels.md), [Verification Model](../02-domain/verification-model.md), [Requirements](../01-product/requirements.md), [Domain Model](../02-domain/domain-model.md), [Vision](vision.md), [Personas](../01-product/personas.md) accordingly.
- **2026-08-16** — Connected the `Professional-Meetups` Flutter repo; reviewed the mock frontend; added CI workflow; exported this vault to the repo's `docs/` and linked it from `CLAUDE.md`, including a flagged onboarding-flow/trust-model gap.
- **2026-08-16** — iOS platform support confirmed in the repo (Xcode/`flutter create` scaffolding, `macos` folder appeared incidentally); recorded as [ADR-007](../04-decisions/adr-007-flutter-as-the-cross-platform-frontend.md); updated [System Architecture](../03-architecture/system-architecture.md), repo `CLAUDE.md`, and this note accordingly.
- **2026-08-16** — Made the vault→repo docs sync a permanent committed script instead of an ephemeral one-off.
- **2026-08-16** — Backend platform ([ADR-008](../04-decisions/adr-008-backend-platform-architecture.md)) and JWT auth strategy ([ADR-009](../04-decisions/adr-009-jwt-auth-strategy.md)) decided; profile-visibility simplified into a subscription-tier rule ([Revenue Model](../01-product/revenue-model.md)) instead of a public/private toggle; account-deletion retention tension flagged as an open item.
- **2026-08-16** — Database hosting decided (Cloud SQL for PostgreSQL, [ADR-010](../04-decisions/adr-010-postgresql-hosting-on-cloud-sql.md)); full backend production-readiness gap list written up and tracked (secrets management, backend CI/CD, deployment region, RPO/RTO, rate limiting/WAF, infra/AppSec threat coverage, backend test strategy, cost monitoring, realtime connection-registry design) instead of left implicit.
- **2026-08-16** — LinkedIn onboarding slice designed ([ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md), PKCE + REST-to-gRPC + minimal schema) and scaffolded in the repo (`backend/`: docker-compose, Dockerfiles, migrations, proto, Go workspace, `PLAN.md` execution brief) — ready to hand to Claude Code.
- **2026-08-16** — Repo reorganization into `frontend/` + `backend/` planned: `CLAUDE.md` and `flutter-ci.yml` updated for the new layout, migration script written (`scripts/migrate_frontend.sh`) to move the Flutter app and split the `.gitignore` — not yet executed/committed on the real repo.
- **2026-08-16** — Formalized `Subscription` as its own domain entity ([Domain Model](../02-domain/domain-model.md), FK to `users`, independent of Trust Level) after a review of the generated `users.sql` queries surfaced that it had been decided in conversation but never actually written into the vault. Also flagged, from that same review, that sole reliance on `linkedin_sub` for account identity is a real gap to close when Level 2 (phone/personal email) is designed — both logged as open items in this note.
- **2026-08-16** — Claude Code substantially completed `backend/PLAN.md`'s onboarding slice (both services, `shared/` packages, tests across unit/component/integration levels) — reviewed the actual code, not just the plan. Added `backend/ARCHITECTURE.md` (file-by-file map of the backend, for debugging/maintenance) and extracted the plan's shape into [Feature Build Plan Template](../06-roadmap/feature-build-plan-template.md) so future backend slices (matching, messaging, safety-gate, SOS) get built with the same scope-boundary/test-pyramid/self-review discipline instead of ad hoc.
- **2026-08-16** — Claude Code finished `backend/PLAN.md` Steps 0-7 (53 tests, real integration test, clean-checkout `docker compose` verification, three self-found-and-fixed bugs). Cowork independently reviewed the actual diff against the security checklist and [ADR-008](../04-decisions/adr-008-backend-platform-architecture.md)/009/010/011 — approved, one non-blocking follow-up noted (see § Currently working on).
- **2026-08-16** — LinkedIn onboarding slice committed (`b727986`, 74 files) and pushed to `origin/develop`. First real backend code in the repo.
- **2026-08-17** — Frontend onboarding rebuild (Steps 0-10) confirmed executed and reviewed by Claude Code, uncommitted; real-device testing then surfaced `invalid_scope_error` (fixed: added missing LinkedIn product) and `401 invalid_client` (root-caused and fixed: LinkedIn's self-serve OIDC product doesn't support PKCE, removed from both frontend and backend, `state`-based CSRF check kept — [ADR-011](../04-decisions/adr-011-linkedin-onboarding-slice-design.md) corrected accordingly). Security review found one Medium info-disclosure issue (raw internal/upstream errors reaching API clients) — confirmed fixed in `apperror.ToGRPCStatus` and `service.go`. Basic onboarding confirmed working end to end. Session-UX/homepage-personalization slice scoped and handed to Claude Code: logout confirmation dialog, homepage personalization (real name/photo), stale "LinkedIn: Not connected" row fix; company name from LinkedIn explicitly requested and explicitly deferred (not available without LinkedIn Partner Program access).
- **2026-08-17** — Level 2/3 verification slice scoped ([ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md)): phone OTP via Firebase Phone Auth, personal email OTP backend-generated, personal details (name/address, matches documented Level 2), corporate email MVP-scoped (OTP + domain capture only, fraud-detection stack deliberately deferred), all skippable with trust-nudge microcopy, same functionality required in `ProfilePage`. Resolved the previously-open single-identity-anchor item: `phone_number`/`personal_email` both get `UNIQUE` constraints. Meetup/Matching/Safety-Gate explicitly scoped out of this pass — flagged as needing its own initiative (new Matching service, full Safety Gate flow), not simple CRUD, per [Domain Model](../02-domain/domain-model.md)/[Roadmap](../06-roadmap/roadmap.md). Updated [Verification Model](../02-domain/verification-model.md) with implementation-status notes, [Project State](project-state.md), `CLAUDE.md`'s Known Gaps section, ADR count references.
- **2026-08-18** — Level 2/3 verification ([ADR-012](../04-decisions/adr-012-level-2-3-verification-delivery-and-identity-anchors.md)) and the session-refresh wiring fix both committed (`b94ad1d`, `c15cf44`).
- **2026-08-18** — Meetup scheduling slice ([ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md)) scoped, planned, and fully built: host-initiated meetups with join requests (supersedes Match→Meetup for Phase 1), trust floor raised to Level 2 for most intents, full Safety Gate wired in, capacity auto-reject verified safe under real concurrency, mock Matches tab fully retired. New `services/meetup` backend microservice, full frontend Schedule/browse/My-Meetups/detail-page build. Uncommitted.
- **2026-08-18** — Map/location provider decided, then reconsidered twice more, landing on a platform split: Apple MapKit (settled) on iOS, Stadia Maps (provisional) on Android — see [ADR-013](../04-decisions/adr-013-host-initiated-meetup-scheduling-with-join-requests.md) §4's second and third corrections. A real address-search bug (UI clipping/paint-order, not an API issue) found and fixed during the platform-split pass. `TESTING-NOTES.md` and its vault pointer [Testing Notes](testing-notes.md) created to track this and the separately-added hardcoded-OTP testing bypass, both flagged as must-not-ship.
- **2026-08-18** — First App Store/Play Store compliance research pass done and checked against the real app ([App Store & Play Store Compliance](../07-research/app-store-and-play-store-compliance.md)) — account deletion, a privacy policy, and Sign in with Apple identified as the highest-priority currently-unmet requirements; the already-flagged "Dating intent selectable but inert" gap connected to a real Play Store age-verification policy, not just scope discipline.
- **2026-08-18** — Second full security/design review pass, covering the meetup-scheduling and location-picker code built since the first pass — see [Action Tracker](action-tracker.md) for current findings.
